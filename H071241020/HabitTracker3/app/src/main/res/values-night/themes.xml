<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark Application Theme Override (Nocturnal Flow Theme) -->
    <style name="Theme.HabitTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Main Brand Colors -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        
        <!-- Status Bar Configuration -->
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Semantic Theme Color Attributes for Dark Mode -->
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorOnBackground">@color/on_background_dark</item>
        <item name="colorOnSurface">@color/on_surface_dark</item>
        
        <!-- Accessibility Compliant Standard Typography Colors Mapping for Dark Mode -->
        <item name="android:textColorPrimary">@color/on_background_dark</item>
        <item name="android:textColorSecondary">@color/neutral_gray</item>
        <item name="android:textColorTertiary">@color/neutral_gray</item>
    </style>
</resources>
